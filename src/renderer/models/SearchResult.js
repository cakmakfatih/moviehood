function SearchResult({desc, peers, provider, seeds, size, time, title}) {
    this.desc = desc;
    this.peers = peers;
    this.provider = provider;
    this.seeds = seeds;
    this.size = size;
    this.time = time;
    this.title = title;
}

export default SearchResult;